[project]
name = "mcp-server"
version = "0.1.0"
description = "MCP Server which interacts with CVG and SQL databases, and provides an API for the MCP Host."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.2",
    "mcp[cli]>=1.28.1",
    "pydantic-settings>=2.14.2",
    "uvicorn>=0.51.0",
]
